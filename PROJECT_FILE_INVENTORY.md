# Project File Inventory

Every important path, with its purpose and handling. Columns:

- **Tracked** — under git version control
- **Gen** — generated by running the project (not authored)
- **Sens** — contains or can contain real vendor PII / credentials
- **Restore** — required to restore a working project from a fresh clone

---

## Handoff & project documentation

| Path | Purpose | Tracked | Gen | Sens | Restore |
|---|---|---|---|---|---|
| `CLAUDE.md` | Operating guide for Claude Code sessions: architecture, commands, conventions, constraints, what not to change | yes | no | no | **yes** |
| `PROJECT_STATE.md` | Current-state snapshot: features, bugs, deps, git state, next steps, do-not-repeat | yes | no | no | **yes** |
| `DECISIONS.md` | Chronological decision record D-01…D-20 incl. rejected approaches and open questions | yes | no | no | **yes** |
| `MIGRATION_HANDOFF.md` | Account-migration handoff and resume instructions | yes | no | no | **yes** |
| `PROJECT_FILE_INVENTORY.md` | This file | yes | no | no | **yes** |
| `ENVIRONMENT_SETUP.md` | How to recreate both environments from scratch | yes | no | no | **yes** |
| `README.md` | User-facing docs: architecture, quick start, config guide, CLI reference, evaluation results, known limitations | yes | no | no | **yes** |

---

## Repository configuration

| Path | Purpose | Tracked | Gen | Sens | Restore |
|---|---|---|---|---|---|
| `.gitignore` | Excludes PII (`uploads/`, `outputs/`, real ground-truth YAML), secrets (`.env`), venvs, caches, editor settings | yes | no | no | **yes** |
| `.gitattributes` | `text=auto eol=lf`; marks `.xlsx/.pdf/.png/.jpg` binary so git does not mangle them | yes | no | no | **yes** |
| `.env.example` | Config template — **placeholders only, no real values** | yes | no | no | **yes** |
| `.env` | **Real config incl. the live Gemini API key** | **NO** | no | **YES** | must be re-created |
| `requirements.txt` | V1 environment pins | yes | no | no | **yes** |
| `requirements-v2.txt` | V2 environment pins (Python 3.12 only) | yes | no | no | **yes** |
| `.vscode/settings.json` | Local editor settings | **NO** | no | no | no |

---

## Application entry & core

| Path | Purpose | Tracked | Gen | Sens | Restore |
|---|---|---|---|---|---|
| `app/main.py` | ASGI entry (`app.main:app`). Import-guards V1, mounts routers + static, calls `configure_logging()`, sets `app.state.v1_available` | yes | no | no | **yes** |
| `app/core/config.py` | `.env` → typed `Settings`. **The only `load_dotenv()` call in the project** | yes | no | no | **yes** |
| `app/core/logging.py` | Root logger config so V2's `getLogger` calls are visible under uvicorn | yes | no | no | **yes** |
| `app/__init__.py`, `app/*/__init__.py` | Package markers (empty) | yes | no | no | **yes** |

---

## HTTP layer

| Path | Purpose | Tracked | Gen | Sens | Restore |
|---|---|---|---|---|---|
| `app/routes/vendor_v1.py` | V1 routes: `/`, `/generate`, `/results/{id}`, `/download/{id}/{kind}`. Owns the in-memory `RUNS` dict | yes | no | no | **yes** |
| `app/routes/vendor_v2.py` | V2 routes: `/v2`, `/v2/process-vendor`, `/v2/results/{id}`, `/v2/download/{id}/{kind}`, `/v2/template-sheets`, `/v2/health`. Owns `V2_RUNS` | yes | no | no | **yes** |
| `app/schemas/vendor.py` | pydantic shapes for the canonical record and verification report. **Currently documentation-only — not used to validate route responses** | yes | no | no | **yes** |
| `app/utils/uploads.py` | `save_upload()` — writes an `UploadFile` to a per-run directory | yes | no | no | **yes** |

---

## Services (business logic)

| Path | Purpose | Tracked | Gen | Sens | Restore |
|---|---|---|---|---|---|
| `app/services/vendor_v1_gemini.py` | **V1 engine.** 3 schema-bound Gemini calls, tenacity retry, PAN reconciliation, 6 regex validators, `XLSX_CELL_MAP`, `fill_vendor_xlsx()`. Also the source `check_config.py` AST-parses. Lines 195–206 hardcode 4 fields to `None` | yes | no | no | **yes** |
| `app/services/vendor_v1_verify.py` | V1 Excel write-back check (integrity, not accuracy) | yes | no | no | **yes** |
| `app/services/vendor_v1_service.py` | Orchestrates V1 for its route: extract → fill each sheet → verify → assemble result | yes | no | no | **yes** |
| `app/services/vendor_v2_service.py` | Orchestrates V2 for its route; raises `V2ServiceError` carrying a user-facing message, detail and hint | yes | no | no | **yes** |

---

## V2 pipeline — `app/pipelines/v2/`

| Path | Purpose | Tracked | Gen | Sens | Restore |
|---|---|---|---|---|---|
| `models.py` | `BBox`, `TextSpan`, `Page`, `Document`, `DocumentSet`, `FieldResult`, `ExtractionResult`; `RENDER_DPI = 200`; JSON round-trip | yes | no | no | **yes** |
| `document_loader.py` | PDF / image / DOCX → spans. **The image-coverage OCR decision (D-10)** | yes | no | no | **yes** |
| `ocr_engine.py` | **Only module importing `paddleocr`.** oneDNN disable at module scope (D-11), engine cache, `drop_score`, reading-order sort | yes | no | no | **yes** |
| `layout_engine.py` | Spans → visual lines; directional neighbour queries in multiples of span height | yes | no | no | **yes** |
| `field_matcher.py` | Scored candidates; inline / adjacent / pattern-only; label-like and foreign-pattern penalties; short-caption threshold escalation | yes | no | no | **yes** |
| `semantic_engine.py` | `DocumentClassifier` + `SemanticEngine`: classify, pool across documents, validation-bias, `_choose()`, two-pass validation, cross-document consistency | yes | no | no | **yes** |
| `validator.py` | Generic executor for 6 rule types incl. `derived` and `dictionary` | yes | no | no | **yes** |
| `dictionaries.py` | Loads and caches word lists for `dictionary` validators | yes | no | no | **yes** |
| `normalizer.py` | Normalisation ops registry incl. `fix_ifsc_confusions` | yes | no | no | **yes** |
| `config_loader.py` | Strict YAML loader; `CONFIG_DIR`; `KNOWN_NORMALIZERS`, `KNOWN_VALIDATOR_TYPES`, `KNOWN_DICTIONARIES` | yes | no | no | **yes** |
| `excel_mapper.py` | YAML-driven cell writing; rejects duplicate cell targets | yes | no | no | **yes** |
| `verifier.py` | Write-back diff + PASS/FAIL cell colouring; handles `"700019.0"` coercion | yes | no | no | **yes** |
| `preprocessing.py` | **Opt-in** denoise/CLAHE/unsharp. Evaluated and rejected as default (D-13). Imports `cv2` | yes | no | no | **yes** |
| `pipeline.py` | Wires stages end to end; CLI with `--cache` to skip OCR | yes | no | no | **yes** |
| `dump_ocr.py` | CLI: inspect raw OCR/document output | yes | no | no | **yes** |
| `check_config.py` | CLI: validate YAML **and assert V1/V2 field-key compatibility** by AST-parsing V1's source. The de facto smoke test | yes | no | no | **yes** |

### Evaluation harnesses — `app/pipelines/v2/eval/`

| Path | Purpose | Tracked | Gen | Sens | Restore |
|---|---|---|---|---|---|
| `eval_extraction.py` | Accuracy scoring vs ground truth; 5 outcome classes; supports `--pipeline v1|v2`; refuses unverified ground truth without `--allow-unverified` | yes | no | no | **yes** |
| `eval_sahi.py` | Baseline vs upscaled vs tiled detection comparison (D-12) | yes | no | no | **yes** |
| `eval_preprocessing.py` | Raw vs preprocessed vs binarised OCR comparison (D-13) | yes | no | no | **yes** |
| `ground_truth/README.md` | Ground-truth schema, the anti-circularity rule, PII warning, scoring outcomes | yes | no | no | **yes** |
| `ground_truth/example_synthetic.yaml` | **Fabricated** ground truth — schema example + CI-safe fixture | yes | no | no | **yes** |
| `ground_truth/mb_control_systems.yaml` | **Real** ground truth for the sample vendor: 24 fields, `conflicts` block documenting the two-bank discrepancy and the vendor-name spelling choice. `verified: false` | **NO** | no | **YES** | optional — see note |

> **Note on `mb_control_systems.yaml`:** gitignored because it transcribes real GSTIN, PAN,
> IFSC and account numbers. It will **not** transfer with a clone. Either copy it across
> manually or rebuild it from `example_synthetic.yaml` + the source PDFs. Its `conflicts`
> block records two business decisions worth preserving: bank details come from the **cheque**
> (not the Udyam certificate), and `vendor_name` uses the GST **Legal Name**.

---

## Configuration data — `app/config/`

| Path | Purpose | Tracked | Gen | Sens | Restore |
|---|---|---|---|---|---|
| `field_dictionary.yaml` | **24 fields.** Captions, regex patterns, normalisation chains, validator names, search directions, `expected_documents`, priority, `required`, `cross_document_consistency`, plus the defaults block with the confidence weights | yes | no | no | **yes** |
| `validation_rules.yaml` | **17 validators** across 6 types, plus the `cross_document` precedence block | yes | no | no | **yes** |
| `document_profiles.yaml` | **5 profiles** (gst_certificate, udyam_certificate, cancelled_cheque, pan_card, bank_statement) with content/filename keyword weights | yes | no | no | **yes** |
| `dictionaries/scheduled_banks.txt` | ~50 RBI-scheduled bank names incl. common short forms | yes | no | no | **yes** |
| `dictionaries/entity_suffixes.txt` | Indian legal-entity suffixes (Pvt Ltd, LLP, & Sons, …) | yes | no | no | **yes** |
| `excel_mappings/vendor_creation_v1.yaml` | Field → Excel cell map for the vendor form | yes | no | no | **yes** |

---

## Frontend

| Path | Purpose | Tracked | Gen | Sens | Restore |
|---|---|---|---|---|---|
| `app/templates/base.html` | Layout shell; Bootstrap 5.3 via CDN; links `/static/style.css` | yes | no | no | **yes** |
| `app/templates/index.html` | V1 upload form; fixed 6-sheet checkbox list; select-all; spinner | yes | no | no | **yes** |
| `app/templates/results.html` | V1 results: summary cards, extracted table, verification table, downloads | yes | no | no | **yes** |
| `app/templates/index_v2.html` | V2 upload form; multi-document; mapping + OCR model selectors; **sheet names fetched live** from the uploaded workbook via `/v2/template-sheets` | yes | no | no | **yes** |
| `app/templates/results_v2.html` | V2 results: per-field confidence, validation status, source document, needs-review, verification table, 5 download links | yes | no | no | **yes** |
| `app/templates/error_v2.html` | V2 friendly error page: message, hint, available sheets, collapsible technical detail | yes | no | no | **yes** |
| `app/static/style.css` | Minimal shared CSS | yes | no | no | **yes** |

---

## Legacy

| Path | Purpose | Tracked | Gen | Sens | Restore |
|---|---|---|---|---|---|
| `legacy/vendor_form_extractor_ocr.py` | **Superseded** Tesseract + regex prototype. Imported by nothing. Kept because its header records *why* that approach was abandoned (D-02) | yes | no | no | reference only |

---

## Runtime data — all gitignored, all sensitive

| Path | Purpose | Tracked | Gen | Sens | Restore |
|---|---|---|---|---|---|
| `uploads/<run_id>/` | Uploaded documents per run — **real vendor PDFs**. ~48 run directories present locally | **NO** | **yes** | **YES** | no |
| `outputs/<run_id>/` | Per-run artefacts: `result.json`, `extraction.json`, `document_set.json`, `vendor_filled.xlsx`, `verification_report*.json`. ~40 run directories locally | **NO** | **yes** | **YES** | no |
| `outputs/eval/v1_accuracy.json` | V1 accuracy scoring output. **Numbers preserved in `PROJECT_STATE.md`**; raw file will not transfer | **NO** | **yes** | **YES** | no |
| `outputs/eval/v2_accuracy.json` | V2 accuracy scoring output. Same handling | **NO** | **yes** | **YES** | no |
| `outputs/gt_ocr_cache/` | Cached OCR document set used to speed up repeated evaluation runs | **NO** | **yes** | **YES** | no |
| `outputs/v2_ocr/`, `outputs/v2_run/` | Earlier CLI run artefacts | **NO** | **yes** | **YES** | no |
| `uvicorn_out.log`, `uvicorn_err.log` | Empty leftover server logs (ignored via `*.log`) | **NO** | **yes** | no | no |
| `.venv-paddle/` | Python 3.12 virtual environment for V2 (~GB) | **NO** | **yes** | no | no — recreate |
| `__pycache__/`, `*.pyc` | Bytecode caches | **NO** | **yes** | no | no |

---

## External to the repository

Not in git; must be transferred or re-created separately.

| Item | Location | Notes |
|---|---|---|
| Sample vendor documents | `C:\Users\shubh\Downloads\Sample vendor documents (4)\Sample vendor documents\` | GST certificate, cancelled cheque, Udyam certificate, and `VENDOR CREATION REQUEST FORM.xlsx` (6 sheets: NSIND, NSITSL, SEBIZ, Smarter, NSV, CSIND). Copies also under `uploads/<run_id>/`. Needed to reproduce any evaluation |
| Gemini API key | gitignored `.env` | **Must be rotated** — was exposed in a terminal session and chat transcript |
| PaddleOCR model weights | `~/.paddlex/official_models/` | ~100 MB, downloads automatically on first V2 run |
