[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Import the project's packages (v2, eval, app) from the repo root without an
# install step -- the app is run in place, not pip-installed.
pythonpath = .

addopts = --strict-markers --tb=short
