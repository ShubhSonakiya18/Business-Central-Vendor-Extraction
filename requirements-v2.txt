# V2 pipeline dependencies (fully local — Python 3.12 required).
# PaddlePaddle has no wheel for Python 3.14, so V2 must run in its own venv:
#
#   py -3.12 -m venv .venv-paddle
#   .venv-paddle\Scripts\Activate.ps1        (Windows)
#   source .venv-paddle/bin/activate          (macOS/Linux)
#   pip install -r requirements.txt -r requirements-v2.txt
#
# requirements.txt (V1) is NOT required to run V2 — install it too only if
# you also want the Gemini pipeline available in the same environment.

paddlepaddle==3.3.1
paddleocr==3.7.0
pypdfium2==5.12.1
rapidfuzz==3.14.5
python-docx==1.2.0
pyyaml==6.0.2

# Shared with V1 but pinned here too so `pip install -r requirements-v2.txt`
# alone is enough to run V2 standalone.
fastapi==0.141.1
uvicorn==0.52.1
jinja2==3.1.6
python-multipart==0.0.32
python-dotenv==1.2.2
openpyxl==3.1.5
